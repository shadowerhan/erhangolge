import Link from 'next/link';import {Heart,ShoppingBag}from'lucide-react';export type P={name:string;slug:string;price:number;old?:number;image:string;tag?:string};export default function ProductCard({p}:{p:P}){return <article className="group"><div className="relative aspect-square overflow-hidden rounded-3xl bg-blush"><img src={p.image} alt={p.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/><button className="absolute right-3 top-3 rounded-full bg-white p-2"><Heart size={18}/></button>{p.tag&&<span className="absolute left-3 top-3 rounded-full bg-brand px-3 py-1 text-xs text-white">{p.tag}</span>}<button className="absolute bottom-3 left-3 right-3 flex translate-y-16 items-center justify-center gap-2 rounded-full bg-ink py-3 text-sm text-white transition group-hover:translate-y-0"><ShoppingBag size={17}/> Sepete Ekle</button></div><Link href={`/urun/${p.slug}`}><h3 className="mt-4 font-medium">{p.name}</h3><p className="mt-1 font-bold text-brand">{p.price.toFixed(2)} TL {p.old&&<del className="ml-2 text-xs font-normal text-gray-400">{p.old} TL</del>}</p></Link></article>}
